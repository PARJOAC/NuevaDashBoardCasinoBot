<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Ajustes - <%= server.name %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <%- include('partials/navbar') %>

    <div class="container mt-5">
      <h2 class="fw-bold mb-4">⚙ Ajustes de <%= server.name %></h2>

      <form
        action="/servers/<%= server.id %>/settings"
        method="POST"
        class="card p-4 shadow-sm"
      >
        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />

        <div class="mb-3">
          <label for="prefix" class="form-label">Prefijo del bot</label>
          <input
            type="text"
            id="prefix"
            name="prefix"
            value="<%= server.prefix %>"
            class="form-control"
            required
          />
        </div>

        <div class="mb-3">
          <label for="mainChannel" class="form-label">Canal principal</label>
          <input
            type="text"
            id="mainChannel"
            name="mainChannel"
            value="<%= server.mainChannel %>"
            class="form-control"
          />
        </div>

        <button type="submit" class="btn btn-primary w-100">
          💾 Guardar cambios
        </button>
      </form>

      <div class="mt-4">
        <% if (server.premium) { %>
        <div class="alert alert-success">
          ✅ Este servidor tiene Premium activo
        </div>
        <% } else { %>
        <div class="alert alert-warning">
          ⚠ Este servidor no tiene Premium.
          <a href="#" class="btn btn-warning btn-sm ms-2">Comprar Premium</a>
        </div>
        <% } %>
      </div>
    </div>

    <%- include('partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
