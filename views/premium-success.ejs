<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Premium Activated - CasinoBot</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      .hero-card {
        max-width: 600px;
        margin: 60px auto;
        background: #fff;
        border-radius: 20px;
        box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
        padding: 40px 30px;
      }
    </style>
  </head>
  <body class="bg-light">
    <%- include('partials/navbar') %>

    <div class="hero-card text-center">
      <h1 class="text-success fw-bold mb-3">Premium Activated!</h1>
      <p class="lead mb-4">
        Your server now has full access to all premium features.
      </p>
      <a href="/dashboard" class="btn btn-primary btn-lg rounded-pill">
        Back to Dashboard
      </a>
    </div>

    <%- include('partials/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
